[package]
name = "jiyunext"
version = "0.1.0"
edition = "2021"
rust-version = "1.76"
build = "build.rs"

[features]
default = []
ffi_bridge = []

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
