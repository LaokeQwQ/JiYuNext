# JiYuNext 重构任务板

## 目标声明
- 技术栈：Rust（主程序与原生 UI）+ Go（核心能力库）
- 运行方式：纯本地，不依赖服务器
- 发布方式：`JiYuNext-x86.exe`、`JiYuNext-x64.exe`（单文件运行）
- 质量目标：发布时零已知 P0/P1 缺陷

## Phase 0 - 仓库骨架初始化
- [ ] 建立新目录结构：`app/`（Rust）、`core/`（Go）、`docs/`（设计与验收）
- [ ] 初始化 Rust 工程（workspace）与 Go module（库模式）
- [ ] 建立统一版本号与构建元数据方案（版本、commit、构建时间）
- [ ] 配置基础 CI（仅构建与静态检查，不发布）

### 验收标准
- 目录结构清晰，能独立执行 Rust/Go 基础构建命令
- CI 至少能完成一次 x86/x64 构建流水验证

## Phase 1 - Rust/Go 接口与调用链
- [ ] 定义 Go C ABI 导出接口：
  - `jy_init(config_json)`
  - `jy_execute(action_json)`
  - `jy_shutdown()`
- [ ] 在 Rust 侧完成 FFI 封装层（错误码映射、超时控制、日志透传）
- [ ] 约定命令数据协议（JSON）：
  - 请求：`action`、`payload`、`trace_id`
  - 响应：`code`、`message`、`result`、`trace_id`
- [ ] 建立崩溃保护策略（panic/异常边界与最小恢复机制）

### 验收标准
- Rust 可稳定调用 Go 导出接口并完成成功/失败双路径测试
- JSON 协议在单元测试内覆盖非法参数与边界输入

## Phase 2 - 功能迁移（尽量全量复刻）
- [ ] 从历史能力基线建立功能清单（按优先级分层：核心/增强/实验）
- [ ] 先迁移核心功能路径，再逐项迁移增强能力
- [ ] 每迁移一项都写“行为对照表”（旧行为 vs 新行为）
- [ ] 对暂不迁移项输出“替代方案或延期原因”

### 验收标准
- 核心功能全部可用
- 非核心功能均有明确状态（已迁移/延期/替代）
- 行为差异可追踪，无“未说明缺口”

## Phase 3 - Win7 优先原生 UI 现代化
- [ ] 建立统一设计 token（颜色、字号、间距、动效）
- [ ] 实现基础组件库（窗口框架、按钮、列表、状态提示、设置面板）
- [ ] 建立可访问性与键盘操作基线（焦点、快捷键、可读性）
- [ ] 输出 UI 状态规范（空态、加载态、错误态、成功态）

### 验收标准
- Win7 环境下主流程 UI 可用且无关键渲染异常
- UI 组件具备一致交互与状态反馈

## Phase 4 - 单文件与体积优化
- [ ] 评估并确定 Rust/Go 静态链接与裁剪参数
- [ ] 移除非必要资源与调试符号，按发布配置最小化体积
- [ ] 建立体积预算门禁（x86、x64 分别设阈值）
- [ ] 产出标准发布目录与命名规范

### 验收标准
- 生成两个单文件 EXE，均可独立运行
- 产物体积满足预算门禁，构建可复现

## Phase 5 - 稳定性与质量治理
- [ ] 建立分级缺陷规则（P0/P1/P2）与拦截策略
- [ ] 建立内存、句柄、线程泄漏巡检脚本
- [ ] 建立长稳运行回归（连续运行与压力场景）
- [ ] 统一日志规范（结构化日志 + trace_id）

### 验收标准
- 发布前零已知 P0/P1
- 长稳回归通过，无持续性资源泄漏

## Phase 6 - 发布验收与兼容回归
- [ ] 维护者执行 Win7/8.1/10/11 兼容回归（真机或虚拟机）
- [ ] 回填每个平台结果、失败步骤、日志与截图证据
- [ ] 对失败项生成修复任务并回归验证关闭

### 验收标准
- 兼容回归记录完整可审计
- 未关闭问题均有明确处置计划和责任人

## 交付物清单
- `README.md`：开发者导向说明
- `WORKLOG.MD`：实施留痕与交接记录
- `tasks.MD`：本任务板（作为执行主清单）
